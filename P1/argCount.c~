#include <stdio.h>
int main (int argc, char *argv[])
{
  printf ("Nome próprio é %s e nº de argumentos %d\n", *argv, argc);
  int i;
  for (i = 0; i < argc; i++)
    printf ("%d : %s\n", i, argv[i]);
  i = 0;
  while (*argv != NULL)
    printf ("%d : %s\n", i++, *argv++);
  return 0;
}
